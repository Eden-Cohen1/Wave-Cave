{
  "name": "wavecave",
  "version": "1.0.0",
  "description": "wave cave website server",
  "main": "server.js",
  "scripts": {
    "start": "node server",
    "dev": "nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Eden Cohen",
  "license": "ISC",
  "devDependencies": {
    "nodeman": "^1.1.2"
  },
  "dependencies": {
    "date-fns": "^2.30.0",
    "uuid": "^9.0.1"
  }
}
